#include <stdlib.h>
#include <string.h>
#include <stdio.h>

def islower(c):
def uppercase(str):
def print_last_digit(number):
def add(a, b):
def fizzbuzz():
listint_t *insert_node(listint_t **head, int number);
def remove_char_at(str, n):

#endif
